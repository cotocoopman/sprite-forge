## What & why
Briefly describe the change and the motivation.

Closes #

## Checklist
- [ ] `yarn test` passes
- [ ] `yarn typecheck` passes (no `any`)
- [ ] New core logic has a `*.test.ts` next to it
- [ ] Project-shape changes are backward compatible (tolerant default in `core/validation.ts`)
- [ ] New UI strings are wrapped with `useT` and added to `src/i18n.ts`

## Screenshots / GIF
If it changes the UI, add a before/after.
